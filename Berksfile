source "https://supermarket.getchef.com"

# Java included here because rvm cookbook "recommends" it, which seems to 
# mean that Berks doesn't do anything with it on "install" but then complains about
# it being absent on upload.  WTF?
cookbook 'java'

metadata
